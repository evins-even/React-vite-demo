# 🚀 React + Vite + TypeScript 项目

一个现代化的 React 项目，集成了响应式 rem 布局、Redux 状态管理、React Router 路由等功能。

## ✨ 特性

- ⚡️ **Vite** - 极速的开发体验
- ⚛️ **React 19** - 最新的 React 版本
- 🎨 **Arco Design** - 字节跳动的企业级 UI 组件库
- 📱 **响应式布局** - 基于 rem 的完整响应式方案
- 🔄 **Redux Toolkit** - 现代化的状态管理
- 🛣️ **React Router** - 声明式路由
- 💅 **Less** - CSS 预处理器
- 📦 **TypeScript** - 类型安全

## 📁 项目结构

```
my-react-demo/
├── docs/                      # 📚 项目文档
│   ├── README.md              # 文档中心
│   ├── responsive-setup.md    # 响应式配置说明
│   ├── migration-guide.md     # 迁移指南
│   └── examples/              # 示例代码
├── public/                    # 静态资源
├── src/
│   ├── main.tsx               # 应用入口
│   ├── App.tsx                # 根组件
│   ├── assets/                # 资源文件
│   ├── common/                # 公共模块
│   │   ├── components/        # 全局组件（小写）
│   │   ├── config/            # 全局配置（原 Global）
│   │   │   ├── globalFontSize.ts    # 响应式字体
│   │   │   ├── responsiveConfig.ts  # 响应式配置
│   │   │   ├── redux/         # Redux store
│   │   │   └── context.ts     # React Context
│   │   ├── hooks/             # 自定义 Hooks（小写）
│   │   ├── styles/            # 全局样式
│   │   └── utils/             # 工具函数
│   ├── pages/                 # 页面组件（小写）
│   │   ├── login/             # 登录页
│   │   └── responsive-test/   # 响应式测试页
│   ├── routes/                # 路由配置
│   └── type/                  # 类型定义
├── package.json
├── vite.config.js             # Vite 配置
└── tsconfig.json              # TypeScript 配置
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3001](http://localhost:3001)

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 📱 响应式配置

本项目使用了完整的响应式 rem 方案，支持自动适配不同屏幕尺寸。

### 快速使用

在 CSS/Less 中直接写 px，系统会自动转换为 rem：

```css
.container {
  width: 200px;    /* 自动转为 2rem，响应式缩放 */
  height: 100px;   /* 自动转为 1rem，响应式缩放 */
}
```

### 固定大小

使用大写 PX 保持固定大小：

```css
.fixed {
  border: 1PX solid #ccc;  /* 保持 1px，不转换 */
}
```

### 详细文档

查看 [响应式配置完整指南](./docs/responsive-setup.md) 了解更多。

## 🎨 技术栈

### 核心框架
- React 19.1.1
- React DOM 19.1.1
- React Router DOM 7.8.2

### 状态管理
- Redux Toolkit 2.9.0
- React Redux 9.2.0

### UI 组件库
- Arco Design 2.66.5

### 构建工具
- Vite 7.1.3
- TypeScript 5.x

### CSS 处理
- Less 4.4.1
- PostCSS 8.5.6
- postcss-pxtorem 6.1.0
- Autoprefixer 10.4.23

### 工具库
- Lodash 4.17.21
- Axios 1.13.2

## 📖 文档

- [响应式配置快速上手](./docs/responsive-setup.md)
- [响应式完整指南](./docs/responsive-guide.md)
- [快速参考](./docs/quick-reference.md)
- [项目迁移指南](./docs/migration-guide.md)
- [文档中心](./docs/README.md)

## 🔧 配置说明

### Vite 配置

- **端口**: 3001
- **自动打开浏览器**: 是
- **代码分割**: 已配置（react-vendor, router, redux, arco-design）
- **PostCSS**: 自动 px 转 rem

### 响应式配置

- **基准值**: 100px = 1rem
- **设计稿宽度**: 1920px
- **最小字体**: 20px
- **最大字体**: 200px

修改配置：编辑 `src/common/config/responsiveConfig.ts`

## 📝 开发规范

### 目录命名
- ✅ 使用小写 + 短横线：`login-page/`, `user-profile/`
- ❌ 避免大写：`LoginPage/`, `UserProfile/`

### 文件命名
- 组件文件：`ComponentName.tsx` 或 `index.tsx`
- 样式文件：`component-name.less` 或 `index.less`
- 工具文件：`util-name.ts`
- Hook 文件：`use-hook-name.ts`

### 代码风格
- 使用 TypeScript
- 使用函数组件 + Hooks
- 使用 Less 编写样式
- 遵循 ESLint 规则

## 🐛 常见问题

### 响应式不生效？

1. 检查 `globalFontSize.ts` 是否在 `main.tsx` 中引入
2. 确认 `vite.config.js` 中的 `rootValue` 与 `responsiveConfig.ts` 中的 `baseSize` 一致
3. 查看浏览器控制台是否有错误

### 样式丢失？

1. 确认 `.less` 文件已正确导入
2. 检查文件路径是否正确
3. 清除缓存重新启动：`rm -rf node_modules/.vite && npm run dev`

### TypeScript 报错？

1. 检查类型定义文件
2. 重启 TypeScript 服务器
3. 运行 `npm install` 重新安装依赖

## 📦 打包优化

项目已配置代码分割，主要分为：

- **react-vendor**: React 核心库
- **router**: React Router
- **redux**: Redux 相关
- **arco-design**: UI 组件库

可以在 `dist/stats.html` 查看打包分析报告。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

ISC

---

## 🎯 下一步计划

- [ ] 完成项目结构优化（参考 [迁移指南](./docs/migration-guide.md)）
- [ ] 添加单元测试
- [ ] 添加 E2E 测试
- [ ] 完善文档
- [ ] 添加 CI/CD

## 📞 联系方式

如有问题，请提交 Issue 或联系项目维护者。

